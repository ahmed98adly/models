general:
  data_base_dir: /app/data
  version: 1.0
  description: run sybil pipeline

execute:
- DicomImporter
- SybilRunner
- DataOrganizer

modules:
  DicomImporter:
    meta: 
      mod: '%Modality'

  DataOrganizer:
    targets:
      #- dicom:mod=ct-->[i:sid]/in_data
      - csv-->[i:sid]/sybil.csv
